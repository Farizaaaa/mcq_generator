name: mcq_generator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  app_links: ^6.3.3
  bloc: ^9.0.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  flutter_box_transform: ^0.4.6
  fluttertoast: ^8.2.10
  timer_count_down: ^2.2.2
  scribble_etome_plugin_plus:
    git:
      ref: main
      url: https://git@github.com/team-etome/scribble_plugin_etome_plus.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    
